import { createAction } from '@reduxjs/toolkit';

export const addAllProduct = createAction('ADD_ALL_PRODUCTS');
export const clearProduct = createAction('CLEAR_PRODUCTS');
export const updateProduct = createAction('UPDATE_PRODUCT');
export const removeProduct = createAction('REMOVE_PRODUCT');
