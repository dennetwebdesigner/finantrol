import { createAction } from '@reduxjs/toolkit';

export const addAllMarketplace = createAction('ADD_ALL_MARKETPLACE');
export const clearMarketplace = createAction('CLEAR_MARKETPLACES');
export const updateMarketplace = createAction('UPDATE_MARKETPLACE');
export const removeMarketplace = createAction('REMOVE_MARKETPLACE');
